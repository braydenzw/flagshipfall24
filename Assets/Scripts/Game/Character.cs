using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Character : MonoBehaviour
{
    private Pizza currentPizza; // each character should be able to "own" a pizza
    // TODO: ^this might need to be a GameObject instead, but we would need to enforce that it has a <Pizza> component to accept it

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
